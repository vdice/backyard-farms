import { cutOff } from '../../../helpers/cut-off';
import { module, test } from 'qunit';

module('Unit | Helper | cut off');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cutOff(42);
  assert.ok(result);
});
