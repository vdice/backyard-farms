import { getTitle } from '../../../helpers/get-title';
import { module, test } from 'qunit';

module('Unit | Helper | get title');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getTitle(42);
  assert.ok(result);
});
