
<div class="nav">
  <div class="container navtext">
    <ul class= "pull-left">
      <li><a href="#" class="btn homer">BackYardFarms</a></li>
    </ul>
       <div class="col-sm-3 col-md-3 search">
        <form class="navbar-form" role="search">
        <div class="input-group">
            <input type="text" class="form-control" placeholder="Portland, Or" name="srch-term" id="srch-term">
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </div>
        </div>
        </form>
      </div>
    <ul class= "pull-right">
      <li>{{#link-to 'new-property'}}Add Your Property{{/link-to}}</li>
      {{#if session.isAuthenticated}}
        <li>
          <a {{action "signOut"}}>Sign out</a>
          {{#link-to 'user' session.content.uid}}Your Profile{{/link-to}}
        </li>
      {{else}}
        <li>{{login-modal method="signUp" signUp="signUp" showExtras=true}}</li>
        <li>{{login-modal method="signIn" signIn="signIn" showExtras=false}}</li>
      {{/if}}
      <li><a href="#">Help</a></li>
    </ul>
  </div>
</div>

{{outlet}}
